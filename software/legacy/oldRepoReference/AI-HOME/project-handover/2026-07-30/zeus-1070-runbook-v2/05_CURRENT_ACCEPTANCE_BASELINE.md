# Current acceptance baseline

Must remain true after Milestone 3 work:

- `quali finestre sono aperte?` uses deterministic fast path.
- `quali porte sono aperte?` uses semantic vocabulary.
- `quante automazioni ci sono?` returns total and state breakdown.
- multi-category counts work.
- home report works.
- typo `accesse` is recognised.
- one Home Assistant snapshot per aggregate/report request.
- existing 34 tests remain green.
- one Telegram bot process only.
