# GAIA Toolkit V0.1

Bounded implementation of the accepted/frozen Toolkit boundary.

OBSERVE → PRESERVE EVIDENCE → ANALYZE REQUIREMENTS → EVALUATE CANDIDATES → RECOMMEND

Research is optional, read-only, source-attributed evidence only.
Authorization remains outside the Toolkit. No installation or host mutation is implemented.

Canonical deterministic test command (from this directory):

    PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -m unittest discover -s tests -v
