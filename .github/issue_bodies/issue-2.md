Add `detect-secrets` configuration and `pre-commit` hook to prevent future secrets leaks.

Tasks:
- Add detect-secrets config and baseline scan.
- Integrate `pre-commit` with a detect-secrets check.
- Add CI job to run baseline on PRs.

Priority: high
Est hours: 16
Scrum points: 8
