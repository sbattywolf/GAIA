This repository previously contained the OpenClaw project as a submodule at `external/openclaw`.

The external project is available at:

https://github.com/openclaw/openclaw

To re-add as a submodule:

  git submodule add https://github.com/openclaw/openclaw external/openclaw

We removed the embedded code to keep the GAIA repository lightweight and avoid submodule configuration issues.
