version: '3.8'

services:
  gaia-ollama:
    image: ollama/ollama:latest
    container_name: gaia-ollama-1070
    ports:
      - "11435:11434"
    volumes:
      - ./ollama-data:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_KEEP_ALIVE=5m

volumes:
  ollama-data: